[build]
  nixPkgs = ['nodejs-20', 'nodePackages.pnpm']

[phases.setup]
  nixPkgs = ['nodejs-20', 'nodePackages.pnpm']

[phases.install]
  cmds = ["npm install"]

[phases.build]
  cmds = ["npm run build"]

[start]
  cmd = "npm start"
